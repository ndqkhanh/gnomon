"""Gnomon: harness-aware evaluator + closed evolution loop on a portable Harness IR.

The sundial's gnomon casts the shadow that reveals the hour. In Gnomon, the
attribution engine casts the shadow that reveals which harness primitive
failed — making invisible primitive-level failures legible, and making the
evolution loop's reward signal portable across every agent harness.
"""

__version__ = "0.1.0"
